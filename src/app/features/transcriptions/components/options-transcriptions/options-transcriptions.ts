import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-options-transcriptions',
  imports: [RouterLink],
  templateUrl: './options-transcriptions.html',
})
export class OptionsTranscriptions { }
